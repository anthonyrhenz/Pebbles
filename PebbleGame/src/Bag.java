import java.util.ArrayList;

/**
 * A simple bag class
 */
public class Bag {
    private ArrayList<Pebble> pebbleList;

    // What other attributes should it have?

    public Bag() {
        // probably better to add the pebbleList after creating all bags as empty
    }

    //do we add methods such as takePebble, addPebbles etc? or should that be up to a bagManager (or etc) class?
}
